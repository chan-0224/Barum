# 공공데이터 API 검증 결과 (2026-08-05)

바름(Barum) 백엔드에서 쓸 공공데이터 5종을 실제 호출로 전부 검증한 기록.
구현물: `barum-be/daily_context.py` (기상청 + 에어코리아 통합)

---

## 0. 세 줄 요약

1. **5종 전부 실제 응답 확인.** data.go.kr 인증키 하나로 기상청·에어코리아·식약처 전부 뚫린다.
2. **인계 문서의 최우선 확인 항목 '이명 필드에 값이 있는가' → 값은 있으나 채움률 16.5%.** 다만 이것 때문에 정규화 전략을 바꿀 필요는 없다는 결론(아래 4-1).
3. **성분 조합 금기 정보는 공공데이터에 없음이 확정됐다.** 룰테이블 20개 수기 구축으로 확정.

---

## 1. 검증 환경

- Python 3.14 + httpx (`barum-be/.venv`)
- 인증키: data.go.kr 일반 인증키 **Decoding 키** 1개로 5종 공용
  - httpx·requests는 파라미터를 자동 인코딩하므로 반드시 Decoding 키를 넣어야 한다
  - Encoding 키를 넣으면 `%`가 `%25`로 이중 인코딩되어 실패

---

## 2. API별 결과

### 2-1. 기상청 단기예보 (15084084) — 정상

```
GET https://apis.data.go.kr/1360000/VilageFcstInfoService_2.0/getVilageFcst
params: serviceKey, pageNo, numOfRows, dataType=JSON, base_date, base_time, nx, ny
```

- 실측 응답: `기온 34.0℃, 습도 55%` (서울, 08-05 기준)
- **위경도가 아니라 격자좌표(nx, ny)를 받는다.** Lambert Conformal Conic 변환 구현함
  - 검증: 서울시청 `(60,127)` / 부산시청 `(98,76)` / 제주시청 `(53,38)` / 대전시청 `(67,100)` — 기상청 공식값과 전부 일치
- `base_time`은 02·05·08·11·14·17·20·23시 발표. 발표 +10분 후 제공되므로 **45분 여유를 두고 직전 슬롯**을 고르도록 구현
- 우리가 쓰는 필드: `TMP`(기온) `REH`(습도)

### 2-2. 에어코리아 대기오염정보 (15073861) — 정상

```
GET https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty
params: serviceKey, returnType=json, numOfRows, pageNo, sidoName, ver=1.0
```

- 실측 응답: 서울 40개 측정소, `pm10 22 / pm25 14` (시도 평균)
- **함정 1**: 서비스명이 `ArpltnInforInqireSvc`다. `ArpltnInqirySvc`로 쓰면 `NO_OPENAPI_SERVICE_ERROR`(코드 12)가 뜬다
- **함정 2**: 첫 응답이 **20초를 넘겨서 실제로 타임아웃 났다.** 타임아웃 40초로 잡음
- **함정 3**: 고장난 측정소는 값이 `"-"`, 플래그가 `"통신장애"`로 온다. 필터링 필수
- `ver=1.0`에도 `pm25Value`가 들어 있다(1.3까지 안 올려도 됨)

### 2-3. 식약처 화장품 원료성분정보 (15111774) — 정상 ★핵심

```
GET https://apis.data.go.kr/1471000/CsmtcsIngdCpntInfoService01/getCsmtcsIngdCpntInfoService01
params: serviceKey, pageNo, numOfRows, type=json, INGR_KOR_NAME(선택)
```

**총 21,863건. numOfRows 최대 500** (초과 시 코드 11 에러). 전량 = 44콜, 일일 트래픽 10,000이라 여유.

전량 받아서 필드 채움률 실측:

| 필드 | 의미 | 채움률 |
|---|---|---|
| `INGR_KOR_NAME` | 표준명(한글) | **100.0%** (21,863) |
| `ORIGIN_MAJOR_KOR_NAME` | 기원 및 정의 | 99.7% |
| `INGR_ENG_NAME` | 영문명 | 94.2% |
| `INGR_SYNONYM` | **이명** | **16.5%** (3,609) |
| `CAS_NO` | CAS 번호 | 7.2% |

### 2-4. 식약처 화장품 사용제한 원료정보 (15111772) — 정상

```
GET https://apis.data.go.kr/1471000/CsmtcsUseRstrcInfoService/getCsmtcsUseRstrcInfoService      → 31,191건
GET https://apis.data.go.kr/1471000/CsmtcsUseRstrcInfoService/getCsmtcsUseRstrcNatnInfoService  → 6,630건
```

필드: `REGULATE_TYPE`(금지/제한) `INGR_STD_NAME` `INGR_ENG_NAME` `CAS_NO` `INGR_SYNONYM` `COUNTRY_NAME` `NOTICE_INGR_NAME` `PROVIS_ATRCL` `LIMIT_COND`

- **조합 금기 정보는 없다.** 국가별 배합 금지·배합 한도일 뿐
- 한 성분이 EU/중국/아세안 등 국가별로 행이 쪼개져 있다(그래서 31,191건이지 성분 수가 아님)

### 2-5. 식약처 화장품 관련 정보 (15020628) — 정상이나 쓸모 없음

```
GET https://apis.data.go.kr/1471000/CsmtcsMfcrtrInfoService01/getCsmtcsMfcrtrInfoList01  → 32,473건
```

내용이 **화장품 제조(판매)업체 등록 현황**이다(`ENTP_NAME` `INDUTY` `ENTP_PERMIT_DATE`).
성분 데이터도 아니고 제품 DB도 아니라서, 플랜B(제품명 검색)에도 못 쓴다. **폐기 확정** (아래 4-4).

---

## 3. 발견 ①: 이 DB는 구어체가 아니라 '화장품 성분 표준명'을 쓴다

핵심 성분을 구어체로 조회했더니 0건이 나와서 파봤다:

| 구어체로 조회 | 결과 | 표준명으로 조회 | 결과 |
|---|---|---|---|
| 히알루론산 | **0건** | 하이알루로닉애씨드 | 50건 |
| 아스코르빈산 | **0건** | 아스코빅애씨드 | 10건 |
| 디메치콘 | **0건** | 다이메티콘 | 470건 |

데이터 결함이 아니라 그 반대다. 대한화장품협회 표준명 체계를 그대로 따르고 있고,
**전성분표는 법적으로 이 표준명을 쓴다.**

완전일치 확인한 성분:
`하이알루로닉애씨드` `소듐하이알루로네이트` `레티놀` `나이아신아마이드` `아데노신` `판테놀`
`살리실릭애씨드` `아스코빅애씨드` `다이메티콘` `글리세린` `정제수` `부틸렌글라이콜` — 전부 O

## 4. 그래서 어떻게 할 것인가

### 4-1. 성분 정규화 — 전략 변경 불필요

인계 문서는 "이명이 비어 있으면 LLM 기반 표준명 매핑으로 전환"을 대비책으로 뒀는데, **전환할 필요 없다고 본다.**

- OCR이 읽는 문자열 = 전성분표 = 표준명 = 이 DB의 키. **완전일치 매칭이 주 경로로 성립**한다
- 이명 3,609건의 실제 내용은 `다이메틸 ↔ 디메칠` 같은 **외래어 표기 변형**이라, OCR 오독 보정용 보조 인덱스로는 오히려 적합하다
- 채움률 16.5%가 문제되는 건 이명을 주 경로로 쓸 때뿐이다

다만 **반대 방향에 문제가 생긴다.** 루틴 카드에 "하이알루로닉애씨드 세럼 바르세요"라고 쓰면 사용자가 못 읽는다.
→ **구어체 ↔ 표준명 별칭은 M3 룰테이블에 올라갈 20개 성분에 대해서만 수기로** 붙이면 된다. 어차피 손으로 만들 표라 추가 비용이 거의 없다.

### 4-2. 성분 데이터는 런타임 API 호출 대신 DB 적재 (제안)

- 전량이 44콜이고 갱신 주기가 길다 → 서비스가 매 요청마다 외부 API를 때릴 이유가 없다
- 한 번 받아서 Supabase `ingredients` 테이블에 적재하고 서비스는 DB만 조회
- 이러면 M1 OCR 매칭이 외부 API 지연·장애와 완전히 분리된다 (에어코리아가 20초 걸린 걸 보면 남 얘기가 아니다)

### 4-3. 플랜B 재정의 — 제품 DB 탐색 중단

기존 플랜B는 "OCR 실패 시 제품명 검색"이었으나, **쓸 만한 공개 제품 DB가 없다.**
15020628은 업체 DB고, 나머지 식약처 데이터도 제품 단위가 아니다.

→ **플랜B를 '성분 직접 선택 UI'로 바꾼다.** OCR이 실패하면 사용자가 우리 `ingredients` 테이블(21,863건)에서 성분을 직접 골라 화장대에 등록한다.

이게 오히려 나은 이유:
- 외부 제품 DB 의존이 사라진다. 우리가 이미 가진 데이터만으로 완결
- 데이터 플로우가 하나로 합쳐진다 — OCR이든 수동선택이든 결과물이 똑같이 `ingredients` 참조다
- **CLAUDE.md 원칙 2(보유 제품 밖 언급 금지)가 그대로 유지된다.** 외부 제품명이 끼어들 여지가 없음

### 4-4. 금기 룰테이블은 수기 구축 확정

공공데이터에 조합 금기가 없음이 실측으로 확인됐다. 피부과 문헌·브랜드 공식 사용가이드 기반 20개 내외 수기 구축.
각 룰에 출처를 박아두면 발표 자료로 그대로 쓸 수 있다.

---

## 5. 구현 상태

### 5-1. `barum-be/daily_context.py` — 날씨 컨텍스트

```python
get_daily_context(lat, lon, sido=None) -> {"temp", "humidity", "pm10", "pm25"}
```

실측 결과:
```
selfcheck ok
{'temp': 34.0, 'humidity': 55.0, 'pm10': 23, 'pm25': 15}
1.2초
```

- **UV 미적용 확정.** 생활기상지수 API를 붙이지 않고 반환값에서 `uv` 키를 아예 뺐다. 항상 `None`인 필드를 남겨두면 소비하는 쪽에서 버그가 난다
- **시도 자동 판정.** 광역시도 17개 중심좌표에 최근접 계산 — API 콜 0. `sido` 인자는 매핑이 틀렸을 때 덮어쓰는 용도로만 남겼다
  - 한계: 경계 폴리곤이 아니라 중심점 기준이라 경기 남부가 서울로 붙는 식의 오차가 있다. 미세먼지가 어차피 시도 평균값이라 감수. 정밀도가 필요하면 근접측정소 API(2콜)로 교체
- 기상청·에어코리아 **병렬 호출**(asyncio.gather), 한쪽이 죽어도 나머지는 살린다
- 캐시 키에 시간 버킷이 들어 있어 **TTL 로직 없이** 기상청은 발표시각당 1콜, 에어코리아는 시간당 1콜로 고정
- 격자 변환·발표시각 선택·시도 판정은 네트워크 없이 도는 selfcheck로 검증

### 5-2. `barum-be/schema/ingredients.sql` + `load_ingredients.py` — 성분 사전

```sql
ingredients(id, std_name NOT NULL UNIQUE, eng_name, synonym, cas_no, origin_def, created_at)
```

- `std_name`만 NOT NULL. 나머지는 채움률이 7~99%라 전부 nullable
- 표준명은 21,863건 **전부 고유**해서 UNIQUE 키로 썼다(upsert 기준). 최대 길이 528자라 `varchar` 아닌 `text`
- 참조 데이터라 유저별 격리 대상이 아니다 → **RLS는 켜되 select 전원 허용, 쓰기는 service_role만**
- 적재 스크립트는 44콜로 전량 수집 → 1,000건씩 upsert. `--dry-run` 지원
- dry-run으로 21,863건 수집·정제까지 확인 완료 (실제 적재는 Supabase 프로젝트 생성 후)

---

## 6. 확정된 결정

| # | 결정 | 근거 |
|---|---|---|
| 1 | **UV 미적용** | 생활기상지수는 별도 API. 습도·기온·미세먼지 3축으로 충분 |
| 2 | **시도 하드코딩 최근접 매핑** | API 콜 0으로 전국 대응. 지방에서 열어도 동작 |
| 3 | **성분은 DB 적재 후 DB만 조회** | 런타임에 외부 API를 안 탄다. M1이 API 장애와 분리됨 |
| 4 | **플랜B = 성분 직접 선택 UI** | 제품 DB 탐색 중단. 외부 의존 제거 |
| 5 | **금기 룰테이블 수기 20개** | 공공데이터에 조합 정보 없음이 실측 확정 |

## 7. Supabase 구축 + 격리 검증 (완료)

리전 Seoul(ap-northeast-2). 스키마는 `barum-be/schema/` 에 SQL로 관리한다.

### 7-1. 적재 완료

`ingredients` **21,863건**. 표준명 완전일치 조회(OCR 주 경로) 실측 정상:

```
레티놀            → Retinol
하이알루로닉애씨드 → Hyaluronic Acid  (이명: 히알루론산)
살리실릭애씨드     → Salicylic Acid   (CAS 69-72-7)
정제수            → Water,Aqua
```

### 7-2. 익명 세션 + RLS 격리 검증 — **16/16 통과**

인계 문서에서 "미배정"으로 남아 있던 스파이크. `barum-be/verify_rls.py`로 익명 유저 두 명을 실제로 만들어 때려본 결과다. 실현 가능성 20점의 직접 근거로 쓸 수 있다.

| 검증 항목 | 결과 |
|---|---|
| B는 A의 제품이 조회되지 않음 | PASS |
| B가 A의 제품을 수정/삭제하지 못함 | PASS |
| **B가 user_id를 A로 위조해 삽입하지 못함** | PASS (403) |
| B는 A의 셀카 기록이 조회되지 않음 | PASS |
| B가 성분 사전에 쓰지 못함 (읽기만 허용) | PASS (403) |
| B가 A의 셀카 파일을 못 받음 | PASS (400) |
| **인증 없는 공개 URL로도 못 받음** | PASS (400) |
| B가 남의 폴더명으로 업로드 못함 | PASS (400) |

원칙 4(RLS 격리)와 원칙 5(얼굴 사진 비공개)가 **실측으로 확인됐다.** 검증용 데이터는 자동 정리된다.

### 7-3. 스키마 결정 — `users` 테이블 없음

Supabase 익명 로그인이 `auth.users`에 행을 만들어주고 MVP에 프로필 데이터가 없다.
지금 `users`를 만들면 id만 복사하는 빈 껍데기 + 동기화 트리거 부채가 된다. 격리는 전부 `auth.uid()` 기준이라 무관하게 동작한다. 닉네임·피부타입이 생기면 그때 `profiles`를 추가한다.

- `weather`/`skin_context`/`routine`은 **jsonb** — 셋 다 모양이 아직 바뀐다. 조회 조건으로 안 쓰고 통째로 읽고 쓰기만 해서 손해가 없다
- `product_ingredients.raw_name`은 NOT NULL, `ingredient_id`는 nullable — **OCR 매칭이 실패해도 읽은 원문은 보존**한다. 실패 케이스가 쌓여야 개선된다

---

## 8. M3 룰테이블 담당자에게 넘길 주의사항

적재 후 실측하면서 나온 것들. 룰테이블 20개 만들 때 걸린다.

1. **구어체 별칭은 예상대로 수기로 붙여야 한다.** 룰테이블 후보 12개 중 이명이 있는 건 `하이알루로닉애씨드 → 히알루론산` **하나뿐**이다. 채움률 16.5%가 하필 우리가 쓸 성분들을 비껴갔다.
2. **`아젤라익애씨드`는 DB에 없다.** 표준명 표기가 다르다. → **룰테이블에 넣을 성분은 전부 `ingredients`에 표준명 완전일치로 존재하는지 먼저 검증할 것.** 안 그러면 룰은 있는데 매칭이 안 되는 조합이 생긴다. M3 완료 기준이 "룰테이블 케이스 오답 0"이라 여기서 새면 바로 감점이다.
3. **이명 폴백을 `ilike '%X%'`로 하지 말 것.** `가지추출물`로 찾으면 `잔가지추출물`, `종대황뿌리/줄기/가지추출물`이 딸려온다. 복수 이명은 쉼표로 붙어 있으니 **쉼표 분해 후 완전일치**로 가야 한다.
