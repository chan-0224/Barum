# CLAUDE.md — 바름 (Barum)

매일 아침 셀카 한 장으로, 오늘 날씨와 내 화장대에 맞는 스킨케어 루틴을 30초 만에 받는 서비스.
멋쟁이사자처럼 중앙해커톤 AAC 트랙 · 6인 팀.

**일정: 실개발 마감 8/16 · 제출 8/21(이후 커밋 금지, 적발 시 탈락) · 발표 8/25**

문서: `docs/PLAN.md` `docs/SCREENS.md` `docs/API.md` `docs/DATA.md` `docs/TASKS.md`

## 팀 / 담당

| 담당 | 범위 |
|---|---|
| 왕종휘 | Spring **CRUD만** (제품 목록·등록·삭제) + 올리브영 크롤링 |
| 구교승 | FastAPI AI 서버 (Vision OCR, 루틴 생성, 피부 분석) |
| 이영규 | 프론트 (Next.js 모바일 웹) |
| 임나현 | 디자인 |
| **이찬영 + Claude Code** | **나머지 전부** — 성분 데이터·룰테이블, DB 스키마, 인프라, 배포, 디자인, 그리고 **Spring의 CRUD 외 엔드포인트** |

### Spring 담당 경계 (`spring/`)

| 범위 | 담당 | `docs/API.md` 번호 |
|---|---|---|
| 제품 목록·등록·삭제 (CRUD) | 왕종휘 | 3 · 4 · 7 |
| 그 외 전부 | 이찬영 + Claude Code | 1 날씨 · 2 카탈로그 검색 · 5 업로드 URL · 9~12 기록 · 13 전체 삭제 |
| OCR·루틴 (AI 서버 몫) | 구교승 | 6 · 8 — Spring은 프록시만 |

**프론트 코드(Next.js)는 건드리지 않는다.** Spring은 위 경계 안에서 작업한다.

## 스택

- 프론트: Next.js (모바일 웹, 390px 기준)
- 메인 API: Spring Boot 3.x + Java 21, Gradle — `spring/`
- AI 서버: FastAPI (Python 3.11+) — `barum-be/`
- DB/인프라: Supabase (PostgreSQL + Storage + RLS + 익명 세션)
- AI: GPT-4o Vision
- 외부 데이터: 기상청 단기예보, 에어코리아, 식약처 공공데이터
- 배포: 프론트 Vercel / Spring·FastAPI 컨테이너. 행사 종료까지 상시 가동

## 절대 원칙 (위반 금지)

1. **안전 판정은 결정론적으로.** 성분 금기 조합 판정은 룰테이블이 코드/SQL 레벨에서 선처리한다. LLM은 판정 결과의 자연어 설명만 생성. 금기 판정을 LLM이나 RAG에 맡기는 코드를 작성하지 말 것.
2. **보유 제품 밖 언급 금지.** 루틴 생성 LLM 컨텍스트에는 해당 유저 화장대에 등록된 제품만 넣는다.
3. **의료 표현 통제.** 병명 진단 표현("여드름입니다") 금지, 상태 확인 표현("홍조가 감지됩니다")만. 이상 신호 지속 시 전문의 진료 권장 안내.
4. **로그인 없음.** Supabase anonymous sign-in. 회원가입/로그인 UI를 만들지 말 것. 격리는 RLS(`auth.uid() = user_id`).
   - **Spring은 `service_role` 키를 쓰지 않는다.** 그 키는 RLS를 우회하므로 격리가 앱 코드 품질에 의존하게 된다. 트랜잭션마다 `set local role authenticated` + JWT의 `sub`를 세팅해 **RLS가 실제로 걸린 상태로** 질의한다. `service_role`은 운영 스크립트(`load_*.py`) 전용.
5. **얼굴 사진은 민감정보.** Storage 비공개 버킷 + Signed URL. 공개 버킷 금지.
6. **FastAPI는 DB에 쓰지 않는다.** 읽고 결과만 반환, 저장은 Spring이 담당.
7. **프로덕트는 바름 단독으로 완결.** AAC/WHS 브랜드를 화면·코드에 넣지 않는다. 슈퍼앱 연계는 발표 자료에서만.
8. **UV(자외선지수)는 명시적 제외.** `get_daily_context`는 `{temp, humidity, pm10, pm25}`만 반환. 다시 추가하지 말 것.

## MVP (이것만 만든다 — 이외 기능 제안 금지)

| ID | 기능 | 완료 기준 |
|---|---|---|
| M1 | 제품 등록: 카탈로그 검색(주) + 전성분표 OCR(보조) | 검색 등록 동작, OCR 실물 10종 중 8종 |
| M2 | 데일리 루틴: 셀카 + 날씨 + 화장대 → 루틴 카드, SSE 스트리밍 | 요청→카드 10초 이내 |
| M3 | 성분 충돌 경고: 룰테이블 선처리 + LLM 설명문 | 룰테이블 케이스 오답 0 |
| M4 | 피부 타임라인: 일자별 셀카·루틴 아카이브 | 7일치 조회 |

확장 후보(MVP 완료 후에만): 루틴 카드 인터랙션 폴리싱 → 컨디션 1탭 입력 → 온보딩.
명시적 제외: 푸시 알림, 웨어러블, 소셜, 로그인, 학습 기반 개인화, 파인튜닝.

**기능 추가 판단 기준**: "100초 시연 영상에 이 화면이 나오는가?" 안 나오면 만들지 않는다.

## 현재 상태

**완료**
- 공공데이터 5종 검증, 식약처 성분 21,863건 Supabase 적재
- `daily_context.py` — 기상청+에어코리아 병렬 호출, 격자 변환, 시도 자동 판정, 캐싱
- Supabase 스키마(`ingredients` `products` `daily_records`) + RLS 검증 14개 통과
- 화면 명세(B안), API 명세, 루틴 카드 디자인 시안

**대기 / 진행**
- 성분 금기 조합 룰테이블 (이찬영) — `docs/TASKS.md` TASK 1
- `catalog_products` 스키마 (이찬영) → 수집은 왕종휘
- AI 서버 명세 (구교승 작성 중) — 나오기 전까지 OCR·루틴 생성 구현 보류

## 컨벤션

- 커밋: 한국어 요약 + 기능 ID 태그 (예: `feat: 성분 충돌 룰테이블 (M3)`)
- API: Spring `/api/v1`, FastAPI `/internal/v1`. Pydantic 스키마 필수
- `.env`는 절대 커밋 금지. **Public 레포라 키 한 번 올라가면 재발급밖에 없다**
- UI: 한국어, 2030 타겟, 아침 30초 원-스크린 카드 중심
