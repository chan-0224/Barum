# CLAUDE.md — 바름 (Barum)

매일 아침 셀카 한 장으로, 오늘 날씨와 내 화장대에 맞는 스킨케어 루틴을 30초 만에 받는 서비스.
멋쟁이사자처럼 중앙해커톤 AAC 트랙 · 6인 팀 · **8/21 제출 후 코드 수정 불가(적발 시 탈락), 실질 개발 마감 8/20.**

상세 배경: `docs/바름_기획서_v4.md` · 인계 맥락: `docs/작업_인계.md`
→ **이 두 문서는 레포에 없다.** 심사 대응 전략과 팀원 실명이 들어 있어 Public 레포에서 제외했다(팀 내부 공유). 클론한 환경에는 존재하지 않는 것이 정상.

## 스택

- 프론트: **Next.js (모바일 웹)** — 심사위원이 URL/QR로 폰에서 바로 사용. 모바일 뷰포트 우선, 데스크톱 대응 불필요
- 백엔드: **FastAPI (Python 3.11+)** — AI 오케스트레이션 전용. 외부 API 호출은 async
- DB/인프라: **Supabase** — PostgreSQL + Storage + RLS + 익명 세션
- AI: **GPT-4o** (Vision OCR + 루틴 생성)
- 외부 데이터: 기상청 단기예보(습도·기온), 에어코리아(미세먼지), 식약처 공공데이터(성분 정규화)
- 배포: 프론트 Vercel + 백엔드 컨테이너. 행사 종료까지 상시 가동 필요

## 절대 원칙 (위반 금지)

1. **안전 판정은 결정론적으로.** 성분 금기 조합(레티놀+비타민C, AHA/BHA+레티노이드 등) 판정은 하드코딩 룰테이블이 코드/SQL 레벨에서 선처리한다. LLM은 판정 결과의 자연어 설명만 생성. 금기 판정을 LLM이나 RAG에 맡기는 코드를 작성하지 말 것.
2. **보유 제품 밖 언급 금지.** 루틴 생성 LLM 컨텍스트에는 해당 유저 화장대에 등록된 제품만 넣는다. 미보유·허구 제품 추천이 구조적으로 불가능해야 함.
3. **의료 표현 통제.** 병명 진단 표현("여드름입니다") 금지, 상태 확인 표현("홍조가 감지됩니다")만. 이상 신호 지속 시 전문의 진료 권장 안내로 연결.
4. **로그인 없음.** Supabase anonymous sign-in으로 접속 즉시 익명 세션. 회원가입/로그인 UI를 만들지 말 것. 격리는 RLS(auth.uid() = user_id)로.
5. **얼굴 사진은 민감정보.** Storage 비공개 버킷 + Signed URL. 공개 버킷 금지.
6. **프로덕트는 바름 단독으로 완결.** AAC/WHS 브랜드를 화면에 도배하지 않는다. 특정 회사 맞춤으로 보이면 안 됨 — 슈퍼앱 연계 스토리는 발표 자료(IR Deck)에서만 다룬다.

## MVP (이것만 만든다 — 이외 기능 제안 금지)

| ID | 기능 | 완료 기준 |
|---|---|---|
| M1 | 화장대 등록: 전성분표 사진 → GPT-4o Vision OCR → 성분 파싱 → DB | 실물 10종 중 8종 이상 정상 등록 |
| M2 | 데일리 루틴: 셀카 + 날씨 + 화장대 → 루틴 카드, 스트리밍 출력 | 요청→카드 완성 10초 이내 |
| M3 | 성분 충돌 경고: 룰테이블 선처리 + LLM 설명문 | 룰테이블 케이스 오답 0 |
| M4 | 피부 타임라인: 일자별 셀카·루틴 아카이브 | 7일치 스크롤 조회 |

확장 후보(MVP 완료 후에만): 루틴 카드 인터랙션 폴리싱 → 컨디션 1탭 입력 → 온보딩 → 제품 추천 카드.
명시적 제외: 푸시 알림, 웨어러블, 소셜, 로그인, 밴딧/학습 기반 개인화.

## 데이터 플로우

- **등록**: 라벨 사진 → Storage → FastAPI → Vision OCR → 성분 정규화(식약처 표준명 매핑) → products 테이블
- **루틴**: 셀카 → 비전 컨텍스트(건조/유분/홍조/트러블 유무) + 날씨 컨텍스트 + 보유 제품 조회 → **룰테이블 pre-filter** → 통과 조합만 LLM → 카드 생성 → 스트리밍
- **외부 API**: `get_daily_context(lat, lon) -> {humidity, temp, pm10, pm25}` 형태로 추상화. 소스 교체·캐싱이 이 함수 하나만 건드리면 되게. 에어코리아는 개발계정 트래픽 500이라 지역+시간 단위 캐싱 필수
  - **UV(자외선지수)는 명시적 제외.** 기상청 생활기상지수 별도 API가 필요한데 3축(습도·기온·미세먼지)으로 충분하다고 판단해 반환값에서 뺐다. 다시 추가하지 말 것

## 컨벤션

- 레포: `chan-0224/Barum` (Public, 평가 브랜치 `main`). 현재는 `barum-be/`만 들어 있고, 프론트는 나중에 `barum-fe/`로 합류시킨다. 배포는 main에서 CI 연동 — 미연동 시 감점
- **`.env`는 절대 커밋 금지.** Public 레포라 한 번 올라가면 키 재발급밖에 답이 없다. 푸시 전 스캔 필수
- 커밋: 한국어 요약 + 기능 ID 태그 (예: `feat: 화장대 OCR 파이프라인 (M1)`)
- 브랜치: **`main`은 항상 동작하는 상태로 유지한다.** 심사위원이 여는 화면이자 배포 기준이라 깨진 채로 두지 않는다
  - 기능 작업은 `feat/<기능명>`에서 → PR → main 머지 (6인 병렬 충돌 방지 + PR 히스토리가 심사 자료가 된다)
  - 문서·설정 같은 잔변경은 main 직접 커밋. 하나 고치자고 PR 여는 건 마감 앞두고 낭비
- API: `/api/v1/` prefix, Pydantic 스키마 필수
- 환경변수: OpenAI 키는 해커톤 크레딧이 지급된 Org의 프로젝트 키만 사용
- UI: 한국어, 2030 타겟, 아침 30초에 최적화된 원-스크린 카드 중심. 과한 대시보드/그래프 지양
- 6인 병렬 작업 → **API 계약과 DB 스키마를 코드보다 먼저 확정.** 프론트가 목 데이터로 선행 가능하게

## 판단 기준

기능 추가 여부가 애매하면: **"100초 시연 영상에 이 화면이 나오는가?"** 안 나오면 만들지 않는다.
